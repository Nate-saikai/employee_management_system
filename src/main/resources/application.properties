spring.application.name=EmployeeManagementSystem
server.port=3307
spring.session.timeout=5m

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/OrganizationTable
spring.datasource.username=root
spring.datasource.password=KruZ3rJa670

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Show parameter values (important!)
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Logging
logging.level.org.springframework.security=TRACE

# Encoding
spring.servlet.encoding.enabled=true
spring.servlet.encoding.charset=UTF-8
spring.servlet.encoding.force=true

# Security
spring.security.user.name=Nate-Saika
spring.security.user.password=this-temporary-password
